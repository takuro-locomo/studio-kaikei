# CLAUDE.md — スタジオ会計の音声入力ルール

## このリポジトリの役割

台帳の「正」は Google スプレッドシート「スタジオ会計_入出金管理」（ドライブの 08_経理・出金伝票 内）。
このリポジトリは、そのシートに追記するためのスクリプトと入力ルールだけを持つ。
台帳データを CSV でこのリポジトリに二重に持たないこと。

予約の「正」は Google カレンダー「Studio NEXTE」。予約データもこのリポジトリには持たない。

## 想定する使い方

スマホの Claude Code で音声入力し、内容を 1 行に整形し、復唱して確認を取ってから
`scripts/append_entry.py` でシートに追記する。

入力例：「8月17日、◯◯さん、マシン回数券、12000円、現金でもらった」

（例文の名前と金額は架空のもの。このリポジトリが Public の間は実在の取引先名・実際の金額を書かない）

## 追記先の仕様

- シート名：取引入力
- 列：A 日付 / B 勘定科目 / C 取引先 / D 摘要・内容 / E 入金 / F 出金 / G 支払方法 / H 証凭リンク
- I 列「残高」は ARRAYFORMULA 。**絶対に書き込まない**
- 追記は A～H の 8 列のみ。行の挿入や並べ替えはしない
- 2 行目は期首残高の行。書き換えない

## 音声→行の変換ルール

- 日付：「8/7」「8月7日」は今年の YYYY/MM/DD にする。「きのう」「先週の金曜」も実日付に直す
- **実行環境の時計はずれていることがある**（実際に 5 日ずれていたことがある）。
  「今日」「きのう」「明日」など相対的な日付が出てきたら、`date` の結果を鵜呑みにせず実日付をユーザーに確認する。
  カレンダーの最終更新日時も裏取りに使える
- 金額：数値のみ。カンマや「円」は付けない
- 売上・入金は E 列、経費・支払いは F 列。両方に入れない
- 勘定科目は下の一覧から必ず選ぶ。当てはまるものがなければ勝手に作らずに確認する
- 支払方法：現金 / 銀行振込 / クレジットカード / 口座引落 / 電子マネー・QR決済 / その他
- 言っていない項目は空欄にする。推測で埋めない

## 勘定科目

収入：売上高（スタジオレンタル）、売上高（撮影）、売上高（レッスン）、売上高（物販）、雑収入、事業主借

支出：地代家賃、水道光熱費、通信費、消耗品費、広告宣伝費、支払手数料、旅費交通費、業務委託費、接待交際費、修繕費、保険料、減価償却費、租税公課、新聞図書費、研修費、雑費、事業主貸

レッスンの回数券はとりあえず「売上高（レッスン）」。前受金処理にするかは税理士確認後に見直す。

## 予約カレンダー「Studio NEXTE」との連携

予約は Google カレンダー「Studio NEXTE」に入っている。Claude はコネクタ経由で読み取り、
「今日の予約は？」「今週の予約」と聞かれたら、時間・講師・客名・内容を一覧で答える。

### 講師

**momona / ena / みつこ / 黒岩 / 清水 / yuri / takuro**（takuro は運営者本人）

タイトルに出てくる名前がこの一覧にあれば**講師**、なければ**客**と判断する。
新しい講師が増えたらこの一覧に追記する。判断に迷ったら勝手に決めずユーザーに確認する。

### イベントタイトルの読み方

- `講師名｜メニュー` （例：「◯◯｜無料グループヨガ」）
- `講師名 × 客名｜メニュー` （例：「◯◯ × △△｜マシン回数券 1/4」）
- 名前だけのイベントもある。区切り文字は全角の「｜」
- 「momona」だけ（客名なし）のイベントは講師 momona の**待機（在店）**。予約ではなく、売上も発生しない
- それ以外の名前だけのイベントは、上の講師一覧と照らして判断する。
  一覧にない名前なら**客の予約**。一覧にある講師名なら待機かレッスンか分からないので、記帳前に確認する
- 「無料」と付くイベントは無料レッスン。売上は発生しない
- **体験レッスンは無料**。「無料」と書いていない「体験」「30分体験」も売上は発生しない（有料に変わったらこの記述を直す）
- 「回数券 1/4」のような表記は購入済み回数券の消化。**消化時には売上を計上しない**（売上は券の購入時に計上済み）

#### 予約アプリから自動同期されたイベント

摘要に「予約ID」や「このイベントはアプリから自動同期。変更はアプリ側で」と書かれ、
作成者がサービスアカウント（`...iam.gserviceaccount.com`）のイベントは、予約アプリが作ったもの。

- **カレンダー側で編集・削除してはいけない**（アプリ側の予約とズレる）。読み取りだけにする
- 変更が必要なときは、カレンダーを直さずユーザーにアプリ側で操作してもらう
- 会計の扱いは通常どおり（予約があっても売上とはみなさない）

#### 回数券の「n/総数」表記について

券は 30分単位で数える。種類は **30分8回券** と **30分4回券**。

**分母は「そのお客様が何レッスンで使い切るか」**。券の回数そのものではなく、
レッスンの長さによって決まる（60分レッスンは 30分枠を 2 つ消費する）。

| 券 | レッスンの長さ | 表記 |
|---|---|---|
| 30分8回券 | 60分 | `n/4` |
| 30分8回券 | 30分 | `n/8` |
| 30分4回券 | 30分 | `n/4` |

- n は「何回目のレッスンか」
- 券の種類・レッスンの長さ・n は、すべてユーザーの発言から取る。**推測しない**
- 同じ `n/4` でも券の種類は違うことがある（30分8回券の60分利用と、30分4回券）。
  金額はカレンダーの分母から推測せず、必ずユーザーの発言から取る

### 会計との突き合わせルール

- **予約があっても売上とはみなさない**。無料レッスンや回数券消化があるため、カレンダーから自動でシートに記帳することは絶対にしない
- 売上が発生したときは、ユーザーがその都度コメント（申告）する。その申告を受けてから、通常の音声入力ルール（復唱→OK→追記）で記帳する
- 金額はカレンダーから推測しない。必ずユーザーの発言から取る
- 便利な使い方：一日の終わりに「今日の予約と売上を突き合わせて」と言われたら、その日の予約を一覧にして、売上があった予約をユーザーに確認しながら 1 件ずつ記帳する
- 過去の予約を指した申告もあり得る（例：「8月5日の◯◯さんの予約、回数券買ったから 12000円 現金」）。この場合はカレンダーからその予約を探して**日付と客名（取引先）を確認に使ってよい**。ただし金額と支払方法は必ずユーザーの発言から取り、通常どおり復唱→OK→追記する。該当する予約が見つからなければ、記帳前にどの予約のことか確認する
- 予約の作成・変更・削除は、ユーザーが明示的に頼んだとき（下の「予約の音声入力」を含む）だけ行う。頼まれていないのに勝手に触らない

### 予約の音声入力（カレンダーへの登録）

会計と同じ流れで、音声で予約を受けてカレンダー「Studio NEXTE」に登録する。

入力例：「明日の14時、◯◯さん、体験、講師はmomona」

#### 変換ルール

- 日時：「明日」「来週の火曜」は実日時に直す。終了時刻が言われなければ **1 時間**で登録する。
  ただし**体験は 30分**が既定（既存の体験イベントに合わせる）
- タイトルは既存の書式に必ず合わせる（区切りは全角「｜」）：
  - 講師と客名がある：`講師名 × 客名｜メニュー`（例：`momona × ◯◯様｜体験`）
  - 講師のみ・客名なし：`講師名｜メニュー`
  - 体験は `体験` または `30分体験`。体験は無料なので「無料」を付けなくてよい
  - グループレッスンなど体験以外の無料レッスンはメニュー名に「無料」を含める（売上ゼロと分かるように）
  - 回数券の消化：`講師名 × 客名｜マシンピラティス回数券 n/総数`。
    分母は上の表のとおりレッスンの長さで決まる（60分なら `n/4`）。
    n・券の種類・レッスンの長さはユーザーの発言から取り、推測しない
- 講師・日時のどちらかが言われていなければ、推測せずに確認する

#### 登録前の確認（必須）

- **日時・客名・メニュー・講師**を復唱し、ユーザーが明示的に OK と言うまで登録しない
- 登録前にその時間帯の既存予約を確認し、重なっていたら指摘してから判断を仰ぐ
- 登録後は、入った予約のタイトルと日時を報告する

#### 注意

- 予約を登録しても**売上は計上しない**（会計は上の突き合わせルールどおり、ユーザーの申告があったときだけ記帳）
- 変更・削除も同じ流れ（復唱→OK→実行）。削除は特に慎重に、対象イベントのタイトルと日時を復唱してから

### 注意

- 客名を含む予約内容を、このリポジトリにコミットしない（台帳と同じ扱い）
- カレンダー ID もリポジトリに書かない。カレンダーは名前「Studio NEXTE」で特定する

## 書き込む前の確認（必須）

追記する前に、**日付・勘定科目・取引先・金額・入金か出金か** を復唱して確認を取る。
ユーザーが明示的に OK と言うまでシートに書き込まない。
音声認識は桁を間違える（12,000 → 120,000）ので、金額は特に必ず声に出して確認する。

間違えて追記したときは、シート上で直接修正するか、行を削除する。スクリプトからの削除はしない。

## 証凭（領収書・請求書）

画像や PDF はドライブの 08_経理・出金伝票 内の該当フォルダに保存する。
このリポジトリにはコミットしない。ファイルの URL を H 列に入れる。

## 認証情報の扱い

- サービスアカウントの JSON キーは絶対にコミットしない
- スプレッドシート ID もこのリポジトリに書かない。環境変数で渡す
- 使う環境変数：`SHEET_ID` と、認証はどちらか一方
  - `GOOGLE_SERVICE_ACCOUNT_JSON`：キー JSON の中身をそのまま入れる（スマホ・Web のクラウド環境向け）
  - `GOOGLE_APPLICATION_CREDENTIALS`：キー JSON ファイルのパス（手元の端末向け）

## セットアップ（人間が手でやる）

1. Google Cloud で Google Sheets API を有効化し、サービスアカウントを作成する
2. JSON キーを発行する
3. スプレッドシートの共有に、そのサービスアカウントのメールアドレスを編集者として追加する
4. 環境変数を設定する
   - スマホ・Web（claude.ai/code）で使う場合：claude.ai/code の環境（Environment）設定で、
     `SHEET_ID` にスプレッドシート ID、`GOOGLE_SERVICE_ACCOUNT_JSON` にキー JSON の中身を登録する。
     セットアップスクリプトに `pip install google-api-python-client google-auth cffi` を入れる
   - 手元の端末で使う場合：キーをリポジトリ外に置き、`SHEET_ID` と `GOOGLE_APPLICATION_CREDENTIALS` を設定し、
     `pip install google-api-python-client google-auth cffi` を実行する

## 注意

- 税務の判断（科目の選び方、経費性の有無、前受金処理）は税理士に確認する
- このリポジトリが Public の間は、取引先名や金額を含む内容を一切コミットしない
